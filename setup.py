from setuptools import find_packages, setup

setup(
    name="alloprompt",
    packages=find_packages(include=["allprompt"]),
    version="0.1.0",
    description="",
    author="AlloBrain",
    install_requires=[],
)
